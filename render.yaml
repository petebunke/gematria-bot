services:
  - type: worker
    name: gematria-reply-bot
    runtime: node
    plan: free
    buildCommand: chmod +x build.sh && ./build.sh
    startCommand: node reply-bot.js
    envVars:
      - key: DISCORD_BOT_TOKEN
        sync: false
