services:
  - type: worker
    name: gematria-reply-bot
    runtime: node
    plan: free
    buildCommand: npm install
    startCommand: npx playwright install chromium && npx playwright install-deps chromium && node reply-bot.js
    envVars:
      - key: DISCORD_BOT_TOKEN
        sync: false
